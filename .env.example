# Server
PORT=4000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/dailybloom

# Auth
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=30d

# OTP (placeholder - wire up an SMS provider like Twilio/MSG91 later)
OTP_EXPIRY_MINUTES=5
